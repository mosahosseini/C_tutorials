#include <stdio.h>

int main(){
    char a[] = "M";
    char b   = 'M';
    //printf("is the char of M and string of M the same ? %d" , a==b); 
    printf("This is the %dth element of a strings: %c",1, a[1] );
}